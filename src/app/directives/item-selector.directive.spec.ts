import { ItemSelectorDirective } from './item-selector.directive';

describe('ItemSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
