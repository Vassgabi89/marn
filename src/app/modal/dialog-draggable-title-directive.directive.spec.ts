import { DialogDraggableTitleDirectiveDirective } from './dialog-draggable-title-directive.directive';

describe('DialogDraggableTitleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogDraggableTitleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
